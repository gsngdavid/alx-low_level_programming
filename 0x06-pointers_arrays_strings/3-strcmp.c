#include<string.h>
/**
* _strcmp - compares s1 and s2
* @s1: first string
* @s2: secong string
* Return: returns an integer when s1 is found
*/

int _strcmp(char *s1, char *s2)
{
return (strcmp(s1, s2));
}
