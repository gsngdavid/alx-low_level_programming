#include<string.h>

/**
*  _strcat - for concatinating strings
*  @dest: wher to copy to
*  @src: where to copy from
*  Return: destination string
*/
char *_strcat(char *dest, char *src)
{
return (strcat(dest, src));
}
